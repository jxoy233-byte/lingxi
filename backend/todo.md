- 根据导出文件恢复会话 （暂时不管）

- 回溯后续接统计计时和费用 

- 启动时，提示词按系统自动划分win，mac，linux

- 执行工具自动识别安全性引入human-in-loop    ⚠️ -> 优化对code的审核逻辑现在这个审核逻辑有点太严格了，相当于每次code都要审核 ；  同时在三个选择上再增加一个告诉ai怎么做的选项，把补充的内容直接填进toolmsg里面 

- 图片处理模型默认不使用本地模型，默认和首个传入的模型统一配置（要让图片解析技能也可以使用）

- 前端对话完左侧对应的对话表显示绿色点的完成状态，点击进去一次后就消去；对于出现用户需要审核命令状态时，则对应会话出现小黄点，直到这个审核状态没了消去

- redis库定时清理冗余的checkpoint,只保存每轮对话需要显示的cid

- 控制执行环境 local / sandbox

- *skills可拓展* + 添加定时任务技能（时间/事件 控制） + 信息排队发送 + 规划计划技能（待做）

- 定时清理thinking_chain日志，仅保留最新一天

- 优化记忆文件



- 优化前端界面整体格局

