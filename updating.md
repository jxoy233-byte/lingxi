## 更新内容（v1.0.0）

### 后端更新任务
- 更新了对文本和文档文件文件的处理的功能 (暂时不用干，待办...)
- 更新了带有搜索引擎功能的graph （已完成）
- 更新updated_at字段存放在config的additional_kwargs配置中,这样应该可以解决没有正常显示对话更新时间距离现在的问题（已完成）
- 对于传入文件数据，对后端的Conversation进行拓展变量来保存，使得前端每次对话进入仍可以显示上传过的文件 ***使用additional_kwargs传入每一段对话的文件显示，如果传入了的话，否则置为None***（已完成）
- 在每次对话完后，如果存在搜索，返回搜索相关内容（url，title，content）

### 前端更新任务
- 接收每次对话中显示的搜索内容，并显示在单次对话中（不持久化存储）,（用收敛的形式先显示，如果用户要查看来源就可以点击打开显示得到信息来源，以常见ai对话网站来进行模仿）
- 删除前端显示的两个图标，***同时适当优化前端界面样式（宏观任务）***，更加有美感，简约大气
- *增加Markdown语法渲染*
- 前端的输入消息框改为 Ctrl + Enter 换行
- 优化等待特效
- 增加对话中直接文本复制功能，类似gpt那样的的对话复制
- 前端可以接收更新后端接口中的文件，并可以返回给前端进行对话中持久化显示
- 前端文件显示功能对文件预览显示的大小再进行缩小一点，让其更加美观
- 前端页面每次进入不同对话时进入chat/{session_id}页面，新页面则回到/chat中（已完成）
- 在前端网页更新了不同session_id不同对话页之后，对于发送信息完成后不要调用/chat/conversations接口,而是直接调用当前页面的chat/{session_id}/conversation接口（节约性能消耗），只有每次头次进入时才调用list的接口


### 共同任务
- 实现一个优化用户输入内容的按键功能
- 后端接口的获取对话消息变成了/chat/{session_id}/conversation(已更正)
- 增加可以中断消息发送功能（暂时不用干，待办...）
